export function CreateCpPool({ token }) {
  const ALT_TOKEN = "";
  function createPool() {}

  return (
    <div className="card">
      <h2 className="subtitle">Constant Product Pool</h2>

      <button className="btn blue" onClick={createPool}>
        Create Pool
      </button>
    </div>
  );
}
